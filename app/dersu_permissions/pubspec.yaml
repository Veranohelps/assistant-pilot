name: dersu_permissions
description: Dersu permission plugin.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.example.dersu_permissions
        pluginClass: DersuPermissionsPlugin
      ios:
        pluginClass: DersuPermissionsPlugin
